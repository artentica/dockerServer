version: '2'

services:
    caddy:
        container_name: caddy
        image: florentindubois/infrastructure

        external_links:
            - owncloud
            - portfolio

        ports:
            - "80:80"
            - "443:443"
